var React = require('react');

module.exports = React.createClass({
  render: function() {
    return (
      <div id="about-view">
        <h1>About</h1>
        <p>An example of a React client side app todo app
        that consumes JSON from a Rails API.</p>
      </div>
    );
  }
});
